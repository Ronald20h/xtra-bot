# 🚀 حل مشكلة البريميوم والسيشن — خطوة بخطوة

## المشكلة
Railway بيمسح الملفات كل ما بيعيد تشغيل الـ container.
الحل: إضافة **Volume** في Railway لحفظ البيانات بشكل دائم.

---

## الخطوة 1: رفع الملفات المُصلحة

ارفع الملفات الجديدة على GitHub:
- `server.js` ← تم إصلاحه
- `bot.js` ← تم إصلاحه

---

## الخطوة 2: إضافة Volume في Railway

1. افتح مشروعك في Railway
2. اضغط على الـ **Service** بتاعك
3. روح تبويب **Settings**
4. نزّل لتحت لحد **Volumes**
5. اضغط **+ Add Volume**
6. في **Mount Path** اكتب: `/app/data`
7. اضغط **Create**

---

## الخطوة 3: إضافة متغير بيئة

في Railway → **Variables** أضف:
```
RAILWAY_VOLUME_MOUNT_PATH=/app/data
```

---

## الخطوة 4: زيادة وقت الـ Session

تم التعديل تلقائياً في server.js الجديد:
- Session الآن = **24 ساعة** (بدل 10 دقائق)
- البيانات بتتحفظ في Volume = **دائمة**

---

## ✅ النتيجة

| المشكلة | الحل |
|---------|------|
| البريميوم بيتمسح | Volume دائم في /app/data |
| Session تنتهي بسرعة | 24 ساعة بدل 10 دقائق |
| البيانات تتمسح عند restart | Volume محمي من الـ restart |

